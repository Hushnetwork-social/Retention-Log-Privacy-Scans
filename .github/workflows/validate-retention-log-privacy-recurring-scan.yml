name: Validate Retention Log Privacy Recurring Scan

on:
  push:
  pull_request:

jobs:
  public-only-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate public package
        run: python3 scripts/validate_public_package.py --root .
